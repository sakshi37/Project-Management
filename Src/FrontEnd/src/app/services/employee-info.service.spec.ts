import { EmployeeInfoService } from './employee-info.service';

describe('EmployeeInfoService', () => {
  it('should create an instance', () => {
    expect(new EmployeeInfoService()).toBeTruthy();
  });
});
