import { DailyReportService } from './daily-report-service';

describe('DailyReportService', () => {
  it('should create an instance', () => {
    expect(new DailyReportService()).toBeTruthy();
  });
});
