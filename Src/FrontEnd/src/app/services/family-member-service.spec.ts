import { FamilyMemberService } from './family-member-service';

describe('FamilyMemberService', () => {
  it('should create an instance', () => {
    expect(new FamilyMemberService()).toBeTruthy();
  });
});
