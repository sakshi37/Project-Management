import { DashboardInfoService } from './dashboard-info.service';

describe('DashboardInfoService', () => {
  it('should create an instance', () => {
    expect(new DashboardInfoService()).toBeTruthy();
  });
});
