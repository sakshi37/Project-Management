import { InactiveEmployeeService } from './inactive-employee-service';

describe('InactiveEmployeeService', () => {
  it('should create an instance', () => {
    expect(new InactiveEmployeeService()).toBeTruthy();
  });
});
