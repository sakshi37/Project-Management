import { ErrorHandlerService } from './error-handler.service';

describe('ErrorHandlerService', () => {
  it('should create an instance', () => {
    expect(new ErrorHandlerService()).toBeTruthy();
  });
});
