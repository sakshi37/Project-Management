import { UpdateEmployeeDto } from './update-employee-dto';

describe('UpdateEmployeeDto', () => {
  it('should create an instance', () => {
    expect(new UpdateEmployeeDto()).toBeTruthy();
  });
});
