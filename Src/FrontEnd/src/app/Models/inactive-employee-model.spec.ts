import { InactiveEmployeeModel } from './inactive-employee-model';

describe('InactiveEmployeeModel', () => {
  it('should create an instance', () => {
    expect(new InactiveEmployeeModel()).toBeTruthy();
  });
});
