import { ActivateEmployeeModel } from './activate-employee-model';

describe('ActivateEmployeeModel', () => {
  it('should create an instance', () => {
    expect(new ActivateEmployeeModel()).toBeTruthy();
  });
});
