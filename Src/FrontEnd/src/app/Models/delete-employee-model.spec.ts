import { DeleteEmployeeModel } from './delete-employee-model';

describe('DeleteEmployeeModel', () => {
  it('should create an instance', () => {
    expect(new DeleteEmployeeModel()).toBeTruthy();
  });
});
