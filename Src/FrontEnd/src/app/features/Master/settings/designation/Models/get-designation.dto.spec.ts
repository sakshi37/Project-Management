import { GetDesignationDto } from './get-designation.dto';

describe('GetDesignationDto', () => {
  it('should create an instance', () => {
    expect(new GetDesignationDto()).toBeTruthy();
  });
});
