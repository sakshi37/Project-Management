import { UpdateDesignationDto } from './update-designation.dto';

describe('UpdateDesignationDto', () => {
  it('should create an instance', () => {
    expect(new UpdateDesignationDto()).toBeTruthy();
  });
});
