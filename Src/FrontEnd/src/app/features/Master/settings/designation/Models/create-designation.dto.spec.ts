import { CreateDesignationDto } from './create-designation.dto';

describe('CreateDesignationDto', () => {
  it('should create an instance', () => {
    expect(new CreateDesignationDto()).toBeTruthy();
  });
});
