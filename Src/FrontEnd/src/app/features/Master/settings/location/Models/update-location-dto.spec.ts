import { UpdateLocationDto } from './update-location-dto';

describe('UpdateLocationDto', () => {
  it('should create an instance', () => {
    expect(new UpdateLocationDto()).toBeTruthy();
  });
});
