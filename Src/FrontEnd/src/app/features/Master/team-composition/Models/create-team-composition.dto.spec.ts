import { CreateTeamCompositionDto } from './create-team-composition.dto';

describe('CreateTeamCompositionDto', () => {
  it('should create an instance', () => {
    expect(new CreateTeamCompositionDto()).toBeTruthy();
  });
});
